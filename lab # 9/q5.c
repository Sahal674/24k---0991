#include<stdio.h>
#include<string.h>
char reverse(char array[20], char word[20]){
	for (int j = 20 ; j >= 0 ; j--){
			char ch, word[20];
			ch = array[j];
			if(ch != "\0"){
				strcat(word, ch);
			}
		}
		return 
}
int main(){
	char new[20]; 
	char str = [20];
	printf("enetr a word ");
	scanf("%s", str);
	reverse(str , new);
	printf("%s",new);
	return 0;
	
}